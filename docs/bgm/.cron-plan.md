# Phase 75 — BGM library expansion 10 → 50

Autonomous cron plan. One task per tick. Flip `[ ]` → `[x]` when done.
Skip with `[x-skipped: <reason>]` after 2 same-class failures.

Branch: `phase75/bgm-lib-expand-50` (base: main).
Constraint: only touch `scripts/render-worker/`, `docs/bgm/`, `DEVLOG.md`,
`RELEASE.md`. No app/, no supabase/, no migrations. No mocks. English UI.
Bucket E = instrumental fusion only, no Mandarin vocals.

## Phase A — bootstrap (structure)

- [x] A1: recursive `**/*.mp3` gitignore + create 6 empty bucket dirs
- [x] A2: move existing 10 KML tracks into vibe subdirs, update README table
- [x] A3: write `docs/bgm/vibe-map.md` (6-bucket spec) + this cron-plan

## Phase B — fetch tracks (target 50 total)

Each task: extend `fetch.sh` with N tracks for the named bucket, verify each
URL returns 200 + audio/mpeg via `curl -I`, download to bucket dir, update
`manifest.json` with {title, artist, license, source_url, bucket, duration_s}.
No batch installs, no new deps.

- [x] B1: Bucket A `a-warm-acoustic` — add 4 more KML tracks (target 10)
- [x] B2: Bucket B `b-tropical` — add 8 tracks (KML tropical/island + Bensound)
- [x] B3: Bucket C `c-lofi` — add 7 more tracks (KML jazzy/chill + Bensound lofi)
- [x] B4: Bucket D `d-uplift` — add 6 more tracks (KML corporate/uplift + Bensound)
- [x] B5: Bucket E `e-cn-fusion` — add 8 instrumental fusion tracks (KML asian/world + Bensound cinematic-asian, **no vocals**)
- [ ] B6: Bucket F `f-ambient` — add 7 more tracks (KML ambient + Bensound reflective)

## Phase C — worker + verify + merge

- [ ] C1: create `scripts/render-worker/bgm/manifest.json` (final, all 50 entries with per-track attribution)
- [ ] C2: update `worker.py` `pick_bgm()` — bucket routing via listing metadata; fall back A → silent
- [ ] C3: unit-test bucket routing in `scripts/render-worker/tests/` (no network, no DB) — pure function
- [ ] C4: update `scripts/render-worker/bgm/README.md` — full 50-track table, per-bucket attribution block, `fetch.sh` usage
- [ ] C5: DEVLOG entry + RELEASE note (product-facing: "richer background music variety in generated videos")
- [ ] C6: merge to main — `git status` clean → checkout main → `git merge --no-ff` → push → verify SHA in `git log origin/main --oneline -5` → restart `percho-render-worker` → `systemctl is-active` → delete branch

## Tick Log

- [2026-07-15 bootstrap] A3: created vibe-map.md + this plan
- [2026-07-15 12:00 UTC] B1: added 4 KML tracks (Happy Alley, Balloon Game, Take a Chance, Pookatori and Friends) → bucket A now 10/10; seeded manifest.json
- [2026-07-15 12:14 UTC] B2: added 8 KML tropical tracks (Bossa Antigua, Rainbows, Beach Party, Latin Industries, Samba Isobel, Cuban Sandwich, Salty Ditty, Bumba Crossing) → bucket B 8/8; fetch.sh + manifest.json updated
- [2026-07-15 06:46 UTC] B3: added 7 KML jazzy/chill tracks (George Street Shuffle, Groove Grove, Hep Cats, Jazz Brunch, Smooth Lovin, Local Forecast - Elevator, Faceoff) → bucket C 8/8; fetch.sh + manifest.json updated
- [2026-07-15 07:00 UTC] B4: added 6 KML uplift tracks (Inspired, Fluffing a Duck, Feelin Good, Ready Aim Fire, New Direction, Enter the Party) → bucket D 8/8; fetch.sh + manifest.json updated
- [2026-07-15 07:21 UTC] B5: added 8 KML instrumental fusion tracks (Cambodian Odyssey, Anguish, Water Lily, Meditation Impromptu 01/02/03, Jalandhar, Long Note One) → bucket E 8/8; fetch.sh + manifest.json updated
