# Percho ReelEstate UI rewrite — autonomous plan (15min tick)

**Branch**: `feat/reelestate-ui-rewrite` (never merge yourself, user reviews)
**Spec**: `docs/design/reelestate-teardown/README.md` (single source of truth)
**Screenshots**: `docs/design/reelestate-teardown/screenshots/` (20 PNGs)
**Started**: 2026-07-14

## Rules (every tick MUST obey)

1. **ONE task per tick**, budget 15min, self-kill at 12min if stalled → mark `[x-stalled: reason]` and move on
2. **Never leave the branch** `feat/reelestate-ui-rewrite`. Never merge to main.
3. **Commit after every task** with prefix `[reelestate-ui]` — one task = one commit.
4. **English-only** UI/schema/vars. No `_zh` fields. Marketing copy generators allowed multi-lang.
5. **No mock/seed/demo data**. If a component needs data and none exists, use real Supabase or block the task with a concrete question.
6. **No videos into git** (`*.mp4/mov/webm/mkv` gitignored — verify before commit).
7. **No prettier --write** on existing files. Format only your own new files.
8. **Off-limits paths**: `render-worker/`, `scripts/poi-*.py`, anything under `content-pipeline/`. UI rewrite = `app/`, `components/`, `styles/`, `tailwind.config.ts` only.
9. **Positioning conflicts** (CLAUDE.md vs memory vs README §0): **memory wins**. Log the conflict, don't ask.
10. **Same class of error 2× → skip** with `[x-skipped: reason]`, do NOT loop.
11. **Cite README section** in every commit body (`ref: README §2.3`).
12. **After every task, append one Tick Log line** below.

## Off-limits until user unblocks (§7 open questions)

- ❌ Circles internal UI (README §2.10 — voting UI needs original design, user must draft)
- ❌ Bottom nav 5-slot final selection (currently placeholder 4 slots: Feed / Explore / Messages / Profile)
- ❌ "For Rent" mode toggle (may or may not ship)
- ❌ Rename Circles → Percho name (assume `Circles` string constant until user renames)

## Tasks (priority order — reference README section shown)

### Phase 0: Foundation (2 ticks)
- [x] P0.1 Patch `tailwind.config.ts` with tokens from README §1 (colors/radii/type/spacing/shadow) → commit → verify `pnpm tsc --noEmit` clean
- [x] P0.2 Create `app/(mobile)/` route group + baseline shell (dark bg, safe-area insets, no-op bottom nav placeholder) → verify `pnpm dev` renders empty shell without errors

### Phase 1: Reel Feed hero (§2.1) (4 ticks)
- [x] F1.1 Build `<ReelFeed>` container: full-viewport vertical scroll-snap, real Supabase listings query (limit 5), skeleton loader → verify scrolls on mobile viewport
- [x] F1.2 Build `<ReelCard>` layout: video/photo `object-contain`, price overlay top-left, agent chip top-right, address block bottom-left, action rail right → verify with 1 real listing
- [x] F1.3 Right action rail: like / save / share / comments count buttons w/ cyan glow states — icons from lucide-react
- [x] F1.4 Caption line-heights per README §2.1 (26/13/13/13) — no override, use canonical

### Phase 2: Property Detail (§2.3) (5 ticks)
- [x] D2.1 `app/(mobile)/listings/[id]/page.tsx` — RSC fetch, use `unstable_cache` per supabase-rsc-perf-playbook skill
- [x] D2.2 Hero photo gallery: horizontal swipe, `object-contain` letterbox OK, dot indicators
- [x] D2.3 Price + address block (canonical format from memory: `{street}, {city}, {state}${zip?' '+zip:''}`, K/M format via `formatPrice`)
- [x] D2.4 Specs row (bed/bath/sqft) + description accordion
- [x] D2.5 Agent card + CTA buttons (Message / Call / Save)

### Phase 3: Properties List (§2.2) (3 ticks)
- [x] L3.1 `app/(mobile)/listings/page.tsx` grid: 2-col, dark navy cards, cyan accent per README §2.2
- [x] L3.2 Filter bar sticky top: price / beds / community — no functional filter yet, just UI
- [x] L3.3 Grid caption line-heights per README §2.1 (15/11/11 canonical)

### Phase 4: Agent Profile (§2.5) (3 ticks)
- [x] A4.1 `app/(mobile)/agents/[handle]/page.tsx` — gradient ring avatar, name, brokerage, bio
- [x] A4.2 Tab bar: Reels | Properties (2-col grid each) — use existing feed + list components
- [x] A4.3 Contact CTAs (Message / Call / Website) + verified check badge

### Phase 5: Messages Inbox (§2.7) (3 ticks)
- [ ] M5.1 `app/(mobile)/messages/page.tsx` — card-style rows, unread dot, timestamp
- [ ] M5.2 Property pill badge component (embed a property in a message row) — reuse in detail future
- [ ] M5.3 Thread page shell (list scaffolding only, no real send yet — that needs backend planning)

### Phase 6: Create flows (§2.8, §2.9) (4 ticks)
- [ ] C6.1 Create Reel step 1: source picker (from listing photos / from library) — no upload logic yet, just UI
- [ ] C6.2 Create Reel step 2: caption + tags + music placeholder
- [ ] C6.3 Create Property step 1: MLS Import card (orange accent per README §2.9) + manual entry link
- [ ] C6.4 Create Property steps 2-5 stub screens (skeleton, "coming soon" if not designed)

### Phase 7: Polish + Deploy prep (3 ticks)
- [ ] Z7.1 Global bottom nav (4-slot placeholder): Feed | Explore | Messages | Profile — icons + labels
- [ ] Z7.2 Screenshot each finished screen at 390×844 (iPhone SE) + 430×932 (Pro Max) → save to `docs/design/reelestate-teardown/progress/`
- [ ] Z7.3 Write PROGRESS.md summarizing what's done, what needs user input, what's stubbed — cite each README section

---

## Tick Log

<!-- [YYYY-MM-DD HH:MM UTC] TASK_ID: what was done / commit sha / evidence -->

[2026-07-14 17:08 UTC] A4.3: AgentContactCTAs client (Message/Call/Website 3-col pill row mirroring D2.5 shape — Message → /messages/new?agent=<slug>, Call tel: with disabled-pill when phone null, Website external <a target=_blank rel=noreferrer noopener> with disabled-pill when null); VerifiedBadge inline cyan check next to name (renders only when verified===true); MobileAgent gains optional website/verified — not SELECTed and no mock defaults, agents table has no such columns yet (CLAUDE.md §8: schema change deferred to owner approval), so both stay dormant matching 'no phone → disabled Call' pattern; tsc clean / 0dba45b

[2026-07-14 16:52 UTC] A4.2: AgentProfileTabs client (Reels | Properties tab bar, 2-col grid per tab, canonical 15/11/11 tile caption rig from L3.3, cyan Reel badge on video-backed covers); fetchAgentListings lib (unstable_cache 60s, MOBILE_AGENT_LISTINGS_TAG, anon RLS active-only, listings + listing_videos join to derive has_video, limit 60); mounted below bio in /agents/[handle]; tsc clean / c10be43

[2026-07-14 16:35 UTC] A4.1: mobile /agents/[handle] RSC + `fetchMobileAgent` (unstable_cache 60s, MOBILE_AGENT_TAG, createAnonClient per rsc-perf-playbook, real agents SELECT by slug — public reads agent profile RLS from 0001_init); profile header w/ 88px gradient-ring avatar (initial fallback), name, brokerage, bio (whitespace-pre-line); added `/agents/` to isChromeHidden so mobile layout owns chrome; tsc clean / a243afe

[2026-07-14 16:19 UTC] L3.3: ListingTile caption adopts 15/11/11 canonical rig — price 15px semibold cyan, specs + address 11px each, line-heights match font size for tight 3-line stack (memory §74.14 wins over README sketch); tsc clean / fe46702

[2026-07-14 15:58 UTC] L3.2: ListingsFilterBar sticky top Price/Beds/Community pill chips (lucide ChevronDown caret, bg-bg/90 + backdrop-blur, inert server-component buttons — no click handlers so /listings stays RSC, no filter state, no popovers, README §2.2 visual only); mounted above 2-col grid in /listings page; tsc clean / 3b729c9

[2026-07-14 15:42 UTC] L3.1: mobile /listings 2-col grid — real supabase fetchMobileListings (unstable_cache 60s, MOBILE_LISTING_INDEX_TAG, anon RLS active-only, limit 30), rounded-tile bg-bg-surface cards, 4:5 cover, cyan K/M price, canonical address, empty-state string (no seed); tsc clean / e3084e6

[2026-07-14 15:26 UTC] D2.5: AgentCard client component (gradient-ring avatar w/ initial fallback, name + brokerage row, Message tel-link Save 3-pill CTA row, real saveListing/unsaveListing wiring w/ device_id hydrate); extended fetchMobileListing agents select with brokerage/phone/email; mounted below specs, renders only when listing has joined agent; tsc clean / 1efb48d

[2026-07-14 15:08 UTC] D2.4: SpecsAndDescription client component (dot-separated bd/ba/sqft row, 3-line clamped description with Read more/less accordion, self-guarding empty state); mounted below detail header; tsc clean / 00c6e68

[2026-07-14 14:51 UTC] D2.3: shared `formatPrice` K/M helper in lib/format/price.ts; detail header now shows compressed price ($NK / $N.NM) + canonical `{street}, {city}, {state}${zip?' '+zip:''}` address line; tsc clean / 2cf990f

[2026-07-14 14:33 UTC] D2.2: PhotoGallery client component (snap-x mandatory strip, IntersectionObserver-driven dot indicator, object-contain letterbox, single-photo path skips dots/scroll, zero-photo returns null so page renders neutral placeholder); mounted above info header in detail page with 4:3 aspect frame; tsc clean / 34f1a5c

[2026-07-14 14:12 UTC] D2.1: mobile /listings/[id] RSC + `fetchMobileListing` (unstable_cache 60s, MOBILE_LISTING_TAG, createAnonClient per rsc-perf-playbook, real active listing + agent join + ordered listing_photos hydration, no mock fallback); added `/listings/` to isChromeHidden so mobile layout owns chrome; tsc clean / 7a80b42

[2026-07-14 09:12 UTC] F1.4: ReelCard caption adopts canonical 26/13/13/13 rig (price/specs/address, memory §74.14 wins over README 19/15/14 sketch); removed duplicate top-left PriceChip + orphan PriceChip component; tsc clean / 2471575

[2026-07-14 08:52 UTC] F1.3: aborted stale cherry-pick d0e18d1 (already-applied plan-file sync), popped stash, landed ReelActionRail (like/comment/share/save, real Supabase toggleLike + saveListing wiring, listing_like_counts/saved_listing_counts aggregates in feed query, deviceId hydrate on mount, native share + clipboard fallback, comment slot reserved no-op — no comments table yet); tsc clean / c721b2d

[2026-07-14 08:12 UTC] F1.2: ReelCard w/ price chip, agent chip, address block, action-rail slot over cover; feed page uses ReelCard; tsc clean / 6a221b0

[2026-07-14 07:50 UTC] F1.1: ReelFeed snap container + real supabase listings fetch (limit 5, active, newest) + Suspense skeleton + /feed page; tsc clean / 0f1a914

[2026-07-14 07:35 UTC] P0.2: (mobile) route group + dark shell w/ safe-area insets + no-op 4-slot bottom nav placeholder; tsc --noEmit clean / 2aa4654

[2026-07-14 07:14 UTC] P0.1: dark tokens (bg/cyan/blue/magenta/purple/status), gradients, glow shadows, radii, chip tracking added to tailwind.config.ts; legacy Aman aliases kept / e274f85
