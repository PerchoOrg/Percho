# Archive

Finished process artifacts. Nothing here describes how the product works
today — it records how a particular piece of work was run at the time.

Kept rather than deleted because the reasoning in them is occasionally worth
re-reading, but they are **not** maintained and will drift from the code.
If something here contradicts `docs/` or the code, the code wins.

| Path | What it was |
|---|---|
| `spec-v3-sprint/prompts/` | Agent task prompts for the spec-v3 mobile sprint (tasks 0–5). All shipped; the branches they name are long merged. |
| `spec-v3-sprint/VERIFY-task-*-on-mac.md` | Manual verification checklists for those tasks. |
| `spec-v3-sprint/RUN-task-0.1-fixes.md` | Fix list from the task-0 review pass. |
| `spec-v3-sprint/review-task0-opus5.md` | Review write-up for task 0. |
| `MIGRATION-HANDOFF.md` | Written 2026-08-04 on the EC2 host, immediately before it was terminated, to hand the build over to the Mac mini. The migration is done. |

The living spec those prompts implemented is still maintained at
[`docs/design/spec-v3/`](../design/spec-v3/).
