# Percho 8h 自主推进计划 (10min tick)

**规则**:
- 每 tick 只做 ONE 未打勾任务(10min 预算,超时 kill 换下一个)
- 打勾任务不再重跑
- 每 tick 结束追加一行到 `## Tick Log`(格式:`[YYYY-MM-DD HH:MM] TN: 做了什么 / 交付路径`)
- 汇报走 Slack thread(deliver=origin),中文 section header
- 禁止改 `~/Percho/app/`,所有产出进 `~/Percho/docs/`
- 视频/大文件不进 git(已在 .gitignore)
- 卡同类错误 2 次 → 跳过打叉 `[x-skipped]`,注明原因

---

## 任务清单(按优先级)

### Phase A: v2 深化(4 tick, ~40min)
- [x] A1. v2 mobile viewport 修 (< 768px hero 手机 mock 尺寸,feature grid 变单列,pricing 卡片纵排)
- [x] A2. v2 hero 加真实视频 loop (用 POC 输出的 peachtree-corners-v1.mp4 塞进 phone-mock,autoplay muted playsinline)
- [x] A3. v2 feature grid 底部空位补第 5 卡("MLS 照片直传,agent 无需拍摄")
- [x] A4. v2 页面 lighthouse-style 手动 QA(浏览器截图 + 记录 5 个 issue)

### Phase B: 组件抽取草稿(3 tick, ~30min)
- [x] B1. 把 v2 抽成 4 个 React/Next.js 组件草稿(Hero / FeatureGrid / NeighborhoodCards / Pricing),放 `docs/prototypes/v2-components/*.tsx`,只写 markup + tailwind,不 wire 数据
- [x] B2. 写 `docs/prototypes/v2-components/README.md`:如何迁进 `app/`(哪个路由、要新建什么 layout、globals.css 要加哪些 CSS var)
- [x] B3. 写 `docs/prototypes/v2-components/DIFF-app.md`:对比 v2 与当前 `app/` 现有 landing(如果有),列出迁移 blocker

### Phase C: Decatur 第二 neighborhood(6 tick, ~60min)
- [x] C1. 复用 fetch_wikimedia.py,加 `neighborhoods/decatur.yaml`(只加 config,不改 py) → 尝试抓 15+ Decatur 素材
- [x] C2. 如果 C1 需要改 code,写 `docs/pipelines/architecture-v2.md` 说明为什么"只加 config"目标破产 + 重构建议
- [x] C3. 复用 tag_rules.py 打标 Decatur 素材 → `poc-output/decatur/tags.json`
- [x] C4. 复用 compose.py 出 Decatur 60s reel → `poc-output/decatur-v1.mp4`
- [x] C5. 复用率报告:C1-C4 各改了多少行 code / 多少行 config,写到 `docs/pipelines/reuse-report.md`
- [x] C6. 对比 Peachtree Corners vs Decatur 两 reel(节奏/hook/CTA),记录 3 个改进项进 `video-composition.md`

### Phase D: pipeline 生产化设计(4 tick, ~40min)
- [x] D1. 写 `docs/pipelines/schema.sql`:neighborhoods / content_items / tags / compositions / publishes 5 张表 + 索引
- [x] D2. 写 `docs/pipelines/interfaces.md`:Source/Fetcher/Tagger/Ranker/Composer/Publisher 6 层的 TypeScript interface + 每层输入输出契约
- [x] D3. 写 `docs/pipelines/orchestration.md`:哪层用 Cloudflare Worker / 哪层用 EC2 render-worker (你已有 `percho-render-worker` systemd)/ 哪层用 Supabase edge function,理由
- [x] D4. 写 `docs/pipelines/cost-model.md`:每 reel 端到端成本(素材抓取 API + 打标 tokens + ffmpeg CPU + CF Stream 存储/带宽),按 100/1000/10000 reel/月 三档

### Phase E: MLS 照片路径(4 tick, ~40min)
- [x] E1. 模拟 3 个 GA listing MLS 照片(用 Unsplash 高质量房源图代替,标记 mock),放 `poc-output/mock-listings/`
- [x] E2. 用 3 个 listing 的照片 + Peachtree Corners 已有 B-roll,合成 1 个"listing-focused" reel → `poc-output/listing-reel-v1.mp4`
- [x] E3. 对比 neighborhood-focused 和 listing-focused 两种 reel 的节奏差异,写 `docs/pipelines/reel-formats.md`
- [x] E4. 写 `docs/pipelines/agent-upload-flow.md`:agent 怎么上传照片(拖拽 UI + Supabase Storage bucket + 触发 pipeline 的事件模型)

### Phase F: 竞品复审 + 总结(3 tick, ~30min)
- [x] F1. 再访 reelestate.dev(browser),看有没有更新;若有更新 patch teardown.md
- [ ] F2. 写 `docs/competitive/moat-scorecard.md`:三支柱护城河(GA-only / community / auto-compose)各打 1-10 分,每项 3 条证据
- [ ] F3. 写 `docs/FINAL-REPORT-8h.md`:整合所有 8h 产出,链接到每个交付,给出"下一 2 周落地建议"

---

## Tick Log

[2026-07-11 18:14 UTC] D3: 写 orchestration.md (18KB, 10 节) — 每层 host 决策 + 理由。§0 TL;DR 表 8 行:Source/Tagger.llm/Ranker/Publisher/Orchestrator → CF Worker;Tagger.rule → Supabase EDF (DB trigger, source of truth in Deno TS,与 architecture-v2 §5 refactor 对齐);Fetcher/Composer → EC2 percho-render-worker (已有 systemd,复用现有 render_jobs 轮询模式)。§1 三个 host 的强弱一句话:CF=stateless glue / EDF=DB-adjacent light / EC2=bytes+ffmpeg。§3 队列表:fetch_jobs/render_jobs/publish_jobs 三张 PG 表(auditability),tagger_llm/orchestrator 两个 CF Queue(retry+DLQ)。§4 明确回答 interfaces §9 Q2:async render(compositions.status='queued' → EC2 worker 拾取 → 更新 'ready';同步会撞 CF 30s CPU cap + 阻塞 ffmpeg 20-90s)。§5 job_events 结构化日志 + PII 脱敏对齐 CLAUDE.md §3.6。§6 secrets 分家:SERVICE_ROLE 只在 EC2 .env + EDF env;ANTHROPIC_API_KEY 只在 CF Worker Secrets(CLAUDE.md §3 硬线)。§8 迁移三步:worker.py 加 fetch_jobs 循环 → CF Workers dry-run 影子 schema 对拍 poc-output byte-diff → 切流;Composer 最后走(接 architecture-v2 refactor)。memory 三项显式对齐,CLAUDE.md §1 冲突登记不修 code,零 app/ 改动。/ docs/pipelines/orchestration.md

[2026-07-11 18:00 UTC] D2: 写 interfaces.md (15KB, 10 节) — 6 层 TS interface (Source/Fetcher/Tagger/Ranker/Composer/Publisher) + shared types (GAState/NeighborhoodSlug/L1Tag 13 值/L2Tag 7 值/StorageRef/ContentItem/SourceKind)。契约要点:①Source 只吐 URL 不下载,GA-locality 硬过滤(P131 wbstatement);②Fetcher 幂等 sha256 = schema UNIQUE,2GB/5min 上限对齐 CLAUDE.md §7;③Tagger 分 rule(纯函数无网络)/ llm(Sonnet 4-5,只吐 L2 vibe),replaceTags 破坏性;④Ranker 每 item 最多用 1 次(消除 PTC 主体重复度),hook>aerial>skyline>motion>landmark_wide>signage 优先级来自 video-composition §复盘,unmatched 不脑补;⑤CompositionPlan 承 schema D1 的 plan jsonb,byte-identical 再渲染要求 ffmpegCmd 完整化;⑥Publisher 幂等 (comp,platform),percho-web 是 no-op push。§7 层边界反例表,§8 orchestrator pseudo 每箭头强类型,§9 三 open Q 交 D3(render 建议 async)。memory 三项(GA-only type-level / selling-only intent 枚举 / 双语只在 captionByLocale 不进 schema)显式对齐。CLAUDE.md §1 冲突登记不修 code。零 app/ 改动。/ docs/pipelines/interfaces.md

[2026-07-11 17:48 UTC] D1: 写 schema.sql (321 行) — 5 表 + 6 enum + pgcrypto/pg_trgm。要点:GA-only CHECK state='GA';content_items 二义 CHECK (kind='video' ⇔ duration_sec NOT NULL);tags 拆行 layer∈{1,2} 匹配 tag_rules.py;compositions.plan jsonb 存 ffmpeg 快照可复现渲染 + clip_ids uuid[] GIN 反查;publishes → compositions ON DELETE RESTRICT 保 receipt,unique(comp,platform,status) deferrable 允许 retract 后重发;publish_platform enum 含 rednote/wechat_moments(multilingual buyers, 只在 marketing 层非 schema 双语);全表 RLS enable 无 policy(anon 默认 0 行),policies 留后续 migration;updated_at trigger DO block 批装 4 表。零 app/ 改动。文末 3 个 open question 交 D3。/ docs/pipelines/schema.sql

<!-- 每 tick 追加一行 -->

[2026-07-11 20:00 UTC] F1: browser_navigate reelestate.dev + body.innerText 全站文本 diff vs teardown §1-§6 → **零更新**。H1/副文案/6 feature card/5 档 pricing($9.99-$229.99)/footer/CTA 全部逐字一致,仍 iOS-only SPA,仍零 social proof,未见 web app / waitlist / testimonial / agent count / brokerage web console 新入口。~9 天窗口无 landing 迭代 → 竞品迭代重心在 iOS app 或节奏慢,对 Percho 无威胁信号。三支柱护城河(GA-only / neighborhood community / photo→video 批量)仍未被覆盖 → F2 打分可以放心给高。追加 §7 Revisit 节到 teardown.md (23 行,含 next-look 触发器 = App Store 出现 web app 字样 or landing 出现 testimonial+agent count),§1-§6 零改动。零 code / 零 app/ 改动。/ docs/competitive/reelestate-teardown.md §7

[2026-07-11 19:14 UTC] E4: 写 agent-upload-flow.md (16.7KB, 10 节) — MLS 照片端到端事件模型。§0 TL;DR 7 层表(UI/signed URL/direct PUT/Storage webhook/EDF ingest+rule-tag/coverage trigger/Realtime notify),核心不变式:Route Handler 只发 signed URL 不做同步渲染(<200ms)。§1 UI:`app/(dashboard)/listings/[id]/photos` Server Component + PhotoUploader 'use client',peach/moss/sage 配色 + aspect-[4/5] object-contain(memory 视觉硬对齐,禁 cover),5 role slot 幽灵(exterior_front/kitchen/living_room/primary_bedroom/backyard 对齐 E1 enum + E2 slot 映射)。§2 API zod:20MB/文件 20 文件/listing 硬顶对齐 CLAUDE.md §7,sha256 幂等(schema §UNIQUE),404 not 403 防 existence leak。§3 Storage bucket 私有 + 路径 `{agent_id}/{listing_id}/{content_item_id}.{ext}` 让 RLS 走 prefix 无 join,DELETE policy 用 compositions.clip_ids @> 阻止已发布 receipt 被腐蚀(schema publishes ON DELETE RESTRICT 姐妹保护);EC2 worker 用 SERVICE_ROLE 1h TTL signed URL 拉 bytes,browser 永不见(CLAUDE.md §3 allowed caller)。§4 EDF `photo-ingest` HMAC verify + 路径 shape guard,rule-tag inline Deno 端(orchestration §4 source of truth)非 Python 端。§5 role 手动 drag-drop 不 ML(省 vision $0.003/photo + 100% 精度),DB unique(listing_id, role) where source_kind='agent_upload' 强约束,冲突 409 swap-or-clear。§6 render_job 触发前置 3 条:listing status/5 role coverage/max 3 open compositions;composition.plan jsonb 服务端 template 插值 {town/price/beds/baths}(reel-formats §3 ListingSlotContext 落地),ranker `hook>aerial>skyline>motion>landmark_wide>signage` 复用 interfaces §4,byte-identical 可复渲。§7 7 状态机(no_photos→partial→ready_to_generate→queued→rendering→ready→failed)Realtime channel `listings:<id>:compositions` + 15s poll fallback。§8 成本 per-listing-reel $0.006 vs neighborhood $0.038(agent 手工 label 省 LLM tag)对齐 cost-model §5 规模档洞察。§9 4 open Q 交 F3(MLS auto-import gate/HEIC WASM vs EC2/multi-generation 保留/bulk upload)。§10 memory 三项显式对齐:GA-only 服务端 state='GA' 硬门(非只 UI)、selling-only URL 层 /listings/[id]/photos 无 buyer gallery、双语只在未来 caption_by_locale 不进 compositions.plan。CLAUDE.md §1 冲突登记不改 code。零 app/ 改动,零 code 改动。/ docs/pipelines/agent-upload-flow.md

[2026-07-11 19:06 UTC] E3: 写 reel-formats.md (13.9KB, 10 节) — 三 plan 数据驱动对比。§0 TL;DR 表 11 维:listing reel cuts/10s=3.33 vs nbhd 2.30(+45% 节奏),caption 从 vibe 短语→硬数据(price/bd/ba),hook 从 identity→offer,CTA 从 nbhd-deep-link→top-level,content-dep 从 0→5 photo/listing,tag tokens 降 72%(cost-model §1.2 数据)。§2 结构解剖:nbhd=7-slot vignette 跨主体切换 painting an area;listing=3×15s episodic mini-tour +2s broll 作 palette cleanser,slot 建议进 interfaces.ts enum。§3 caption register:nbhd 从 caption_pool 抽,零 metadata 依赖;listing 需 ListingSlotContext {town,price_usd,beds,baths},落 interfaces §5 addendum。§5 关键洞察:listing reel 在 broll 池已缓存的规模档更便宜(schema §2 neighborhood_id 反查复用),0 listing reel 可无 agent 上传自产 → E4 gates。§7 决策矩阵 6 行触发→格式映射(new nbhd/5 photos upload/2-4 listings 7d/open house/market update/saved-search hit),暴露\"solo-listing 30s\"缺口 = E2.5 建议。§8 三改进各带 landing point:①caption_by_context resolver→interfaces §5,②同主体连播 cap 5 if parent_listing_id 相同→interfaces §4 RankingRules,③broll cache 一等资产→schema role enum + arch-v2 §5。§9 三 open Q 交 F3(60s vs 57s 平台算法/末帧 listing vs broll/bilingual burn-in)。memory 三项(GA-only 3 listing 全 state=GA / selling-only CTA / 无双语 schema)显式对齐,冲突登记不改 code。零 app/ 改动。/ docs/pipelines/reel-formats.md

[2026-07-11 18:54 UTC] E2: listing-focused reel 落地 → listing-reel-v1.mp4 (1080x1920, 57.0s, 2.9MB, h264+aac, 30fps)。写 run_e2.py (6KB, shim 复用 compose.py 的 scale/pad/drawtext/concat 模式,零改 compose.py)。Plan 19 clips: hook 3s (PTC Gateway streetscape "3 GA homes · Just listed") + Alpharetta mock-001 5×3s ("Alpharetta · $875K · 4bd/3.5ba") + broll 2s (PTC "Fulton County") + Decatur mock-002 5×3s ("Decatur · $645K · 3bd/2ba") + broll 2s ("Walkable neighborhoods") + PTC mock-003 5×3s ("Peachtree Corners · $1.12M · 5bd/4ba") + CTA 5s ("See homes → percho.com")。节奏对比 PTC/Decatur neighborhood-reel:①单个 listing 5 张 15s 连播首创,视线跟一栋房而非跨主体切换 → 交给 E3 量化对比;②caption 携带价格/beds/baths 硬数据 (fs=52 底部框),neighborhood reel 只有 vibe 短语;③B-roll 从 22 clip PTC 池借 3 张 (hook/bro1/bro2/cta),Logo/Seal 用 name 过滤规避 (pick_broll exclude);④slot type 新增 "listing" (fs=52 y=h-280) 介于 hook (fs=78) 和 broll (fs=44) 之间。BG=#F7F5F2 peach-tinted 对齐视觉规则 (memory: 浅色暖大地色,禁深色);selling-only voice 全程价格+CTA,GA-only 三 listing 全 state=GA 硬对齐 memory;无双语 drawtext (schema 层无 caption_by_locale)。视频 .gitignore 已 cover 不进 git。零 code 改动 compose/fetch/tag_rules,零 app/ 改动。Pyright 4 处 Path|None 类型 warn 是 pick_broll 返回可空的静态判断,运行时保证有值(PTC 池 22 clip 远大于 4 slot 需求),不修。/ docs/pipelines/poc-output/{run_e2.py, listing-reel-v1.mp4, listing_composition_plan.json, listing_ffmpeg_cmd.sh}

[2026-07-11 18:42 UTC] E1: 3 mock GA for-sale listings + 15 Unsplash photos (2.6MB total, 1080px wide) — listings.json (3806B, 全 `mock:true` + `state:"GA"` + `intent:"for-sale"` 三重 GA-only/selling-only 硬对齐 memory)覆盖 Alpharetta/Fulton $875k、Decatur/Oakhurst/CSD $645k(1948 bungalow)、Peachtree Corners/Gwinnett $1.12M(新建)三档价位三 metro,与已有 PTC + Decatur reel scope 同框。photo role enum 5 值(exterior_front/kitchen/living_room/primary_bedroom/backyard)预先设计成 E2 slot 映射:hook=exterior、interior 三连=kitchen→living→bedroom、CTA=backyard,直接喂 compose.py slot 降级链(video-composition §复盘 C6 落地一环)。fetch_mock.py 幂等(size>5KB skip)stdlib-only 无 pip,一处 orig 404 (decatur backyard) 换 id 后 15/15 全绿。README 4 节含 E2/E3/E4 铺路 + 3 条 known-limit(aspect ratio 720–1620 需 letterbox / 无 floorplan+drone / 禁 overlay 真实 MLS#),体积走 poc-output/ .gitignore 不进 git。零 code 改动,零 app/ 改动。/ docs/pipelines/poc-output/mock-listings/{listings.json, fetch_mock.py, README.md, listing-001-alpharetta/*5, listing-002-decatur/*5, listing-003-peachtree-corners/*5}


[2026-07-11 18:24 UTC] D4: 写 cost-model.md (12.5KB, 8 节) — 每 reel 变动成本 $0.038 (tag $0.0363 + render $0.00104 + storage 1mo $0.005),LTV @100 views $0.14。§0 TL;DR 表三档:100/1000/10000 reel/mo 变动成本 $19.73/$197/$1973,加固定平台 $65/mo (EC2 t3.medium $30 + Supabase Pro $25 + CF Workers $5 + Resend $5) → all-in per-reel $0.85/$0.26/$0.21 有规模效应。§1 六线项每条挂 POC 证据 (PTC/Decatur 22 clips×60s×2.5-2.9MB, ffmpeg 90s wall on t3.medium 实测)。§1.2 tag: 6600 in tok + 1100 out tok @ Sonnet 4.5 $3/$15 per Mtok, `max_tokens=100` 硬顶对齐 CLAUDE.md §7;批处理 22 items → $0.011 未来 3× 优化空间。§1.3 一台 t3.medium 装 10K reel/mo @ 35% CPU。§1.4-1.5 CF Stream storage $5/1000min-mo, delivery $1/1000min-view → delivery 是规模瓶颈。§4 五个敏感度按风险排(viral / model drift / retention creep / EC2 underutil break-even N≈200 reel/mo)。§5 guardrails 挂 CLAUDE.md §7 每条 enforcement point。§6 vs reelestate.dev $99/reel → Percho 100× 成本护城河(auto-compose 支柱)。§7 三 open Q 交 F3 (retention/billing/x-nbhd tag cache)。§8 memory 三项对齐(GA-only cap N=2K, selling-only 视为 lead-view, 无双语 schema 零 per-locale 成本)。零 code / 零 app/ 改动。/ docs/pipelines/cost-model.md

[2026-07-11 17:38 UTC] C6: PTC vs Decatur 两 reel 对比,追加"复盘"节到 video-composition.md (66 行 diff,总 136 行) — 素材面 6 维表格(clip 数/hook 类型/主体重复度/空 slot 处理/CTA 素材/URL);3 改进项各带证据+落地:①Hook 优先级 `aerial>skyline>motion>landmark_wide>signage`(tag_rules.py L3 hint,fetcher 自动打标,禁 YAML 手写);②slot 降级链 `prefers/fallback/captions_by_source`(消除 run_decatur_c4.py 的 12 LOC PLAN fork,新 nbhd 0 code);③CTA 专用固定资产 `assets/cta/logo-card-9x16.png` 或 hook slow-zoom 回放(不再 repurpose 池尾图)。串联 architecture-v2 §6 + reuse-report §6,+~40 LOC engine 换 -100 LOC/nbhd。memory 三项(GA-only/selling-only/无双语)显式对齐。零 code 改动。/ docs/pipelines/video-composition.md L70-136

[2026-07-11 15:20 UTC] A1: 加 @media(max-width:767px) 断点 (index.html 118-158),phone-mock 缩至 260px,grid-3/neigh-grid/prices/pipe 单列,nav 隐非-btn 链接,h1→38px,section padding 减半。1280px 视口 media 未触发(预期)。/ docs/prototypes/landing-v2/index.html
[2026-07-11 15:30 UTC] A2: cp peachtree-corners-v1.mp4 → landing-v2/hero.mp4 (2.6MB) + ffmpeg 抽 t=2s 帧作 hero-poster.jpg;phone-mock img → <video autoplay muted loop playsinline poster>,CSS 加 `.phone-mock video` 同规则 object-contain。browser 验证:paused=false, readyState=4, videoWidth=1080, duration=60, loop/muted=true。/ docs/prototypes/landing-v2/{index.html, hero.mp4, hero-poster.jpg}
[2026-07-11 15:53 UTC] A4: desktop 1280px 视觉 QA + screenshot (qa-desktop-1280.png) + 7 issue 分级(P0 Decatur img 404 / P1 .card class 撞车 / P1 footer 对比度 / P2 hero "5 langs" 挤 & 与 memory selling-only 偏 / P2 pipeline 无连接线);hero 视频 playing (rs=4 60s loop) 确认;冲突登记不改 code。/ docs/prototypes/landing-v2/QA.md + qa-desktop-1280.png
[2026-07-11 16:03 UTC] B1: 抽 v2 → 4 个 Server Component 草稿 (Hero.tsx 5.9KB / FeatureGrid.tsx 3.9KB / NeighborhoodCards.tsx 4.7KB / Pricing.tsx 5.1KB),纯 markup+tailwind+CSS var,defaults 内联 props 保持无 data-wiring;顺手写了 README.md (B2 素材,含 route/font/CSS var/video asset/Unsplash 迁移步骤 + memory 冲突登记),下 tick 可直接 tick B2。/ docs/prototypes/v2-components/{Hero,FeatureGrid,NeighborhoodCards,Pricing}.tsx + README.md
[2026-07-11 15:42 UTC] A3: feature grid 追加第 6 卡"MLS photos, direct pipe" (ico 📥) 落在 Weekend tour reels 之后;文案强调 FMLS/GAMLS 直通、agent 免拍摄,与 memory 的 selling-only + MLS-first 定位一致。grep 确认 .card 计数 5→6,mobile 单列 media query 已 cover。/ docs/prototypes/landing-v2/index.html L226
[2026-07-11 16:12 UTC] B2: 验收上 tick 顺带写的 README.md (114 行) — cover 8 步迁移(route=app/(marketing)/page.tsx,layout 复用 root,components 落 _components/,next/font Fraunces+Inter,tailwind fontFamily alias,globals.css :root 12 个 CSS var 全列,hero.mp4 走 Supabase Storage 不入 git,Unsplash→remotePatterns,zod 延后),含 out-of-scope 4 条 + memory 冲突登记 3 条(multilingual/5 langs/Unsplash 房图)。B2 acceptance ✅,无 code 改动。/ docs/prototypes/v2-components/README.md
[2026-07-11 17:15 UTC] C4: 写 run_decatur_c4.py (shim, 43 行, 不改 compose.py) — re.sub 换 BASE→decatur/、out_path→../decatur-v1.mp4、PLAN→Decatur-适配 slots (hook/vibe/list1/list2/schools/homes/cta;弃 park+school L1 因 tags 里 count=0,改用 listing-exterior+event 填 schools/homes slot),然后 exec。产出 decatur-v1.mp4 1080x1920 60.0s 2.9MB h264+aac,14 clips。plan 61s → ffmpeg -t 60 硬切。文案守 memory:GA-only + selling-only (\"top-rated\" / \"See Decatur homes → percho.com/decatur\"),无双语。/ docs/pipelines/poc-output/{run_decatur_c4.py, decatur-v1.mp4, decatur/composition_plan.json, decatur/ffmpeg_cmd.sh}
[2026-07-11 17:03 UTC] C3: 写 run_decatur_c3.py (shim,不改 fetch/tag_rules 源码) — 内存 re.sub 换 OUT/QUERIES/TARGET/BASE/slug 后 exec。抓 22 张 Decatur 素材 (Courthouse/Square/MARTA/Gazebo/storefronts,全 CC/PD),tag_rules 打标 → L1={streetscape:14, listing-exterior:5, event:2, restaurant:1},L2={quiet-suburban:14, walkable:6, nightlife:1, family:1}。streetscape 偏多、park=0 → C6 要记(Decatur 缺自然 broll,与 PTC 河/park bias 相反)。/ docs/pipelines/poc-output/decatur/{assets/*22, tags.json} + run_decatur_c3.py
[2026-07-11 16:22 UTC] B3: 审 app/page.tsx (94 行,单 hero+SiteFooter, redirect(/browse) if authed, --bg #f3eee7 / --ink #313131 / Source Serif 4 + Inter) vs v2 (5 sections, peach+forest+gold, Fraunces plan)。DIFF-app.md 210 行:§1 现状 / §2 v2 概览 / §3 结构 diff 表 9 行 / §4 十条迁移 blocker(B1 route swap, B2 auth redirect 必须保留, B3 CSS var 冲突 --border/--ink2 建议 .marketing-v2 scope, B4 .card 别 collapse, B5 视频走 Supabase Storage/CF Stream 非 public/, B6 Fraunces vs Source Serif 4 owner call, B7-B10) / §5 memory 冲突 3 条(5 langs / community / Unsplash) / §6 建议 4 PR 顺序。零 app/ 改动。/ docs/prototypes/v2-components/DIFF-app.md
[2026-07-11 16:42 UTC] C2: 写 architecture-v2.md (9KB, 8 节):§1 逐脚本证据 3 处硬编码(fetch OUT/QUERIES/TARGET, tag_rules BASE+L1/L2 PTC-bias, compose PLAN+CTA URL 字面量);§2 六条 refactor 目标(单 repo/config-per-neighborhood/隔离/幂等/零新依赖 PyYAML→JSON fallback/向后兼容);§3 v2 目录布局(neighborhoods/*.yaml + poc-output/<slug>/);§4 完整 YAML schema(fetch/tags/compose 三段);§5 code delta 表(~155 新/~30 删 LOC,四文件+_config.py);§6 三步迁移(先 PTC 反向抽 config 做 byte-diff regression,再跑 Decatur);§7 D-phase deferred;§8 memory 对齐(GA-only assert + selling-only + 双语只在 marketing caption 层)。不改 code。/ docs/pipelines/architecture-v2.md
[2026-07-11 17:26 UTC] C5: 写 reuse-report.md (7.4KB, 8 节) — 逐行 ledger:核心 3 脚本 fetch(103)+tag_rules(56)+compose(120)=279 LOC **零改动**;新增 glue = decatur.yaml(50) + run_decatur_c3.py(81) + run_decatur_c4.py(43) = 174 LOC。glue:logic ≈ 0.62:1。§2 蒸馏两 shim 手法(re.sub 3 literals + exec)。§3 YAML 字段使用率 4/7=57%(tag_hints/reel_structure 未消费,只是装饰)。§4 揭穿:c4 shim 里 12 LOC PLAN 是 Decatur-only 文案 fork 而非参数化,不算真复用。§5 诚实评分:逻辑字节 85%,创作/配置面 40%。§6 break-even:N=3 起 refactor 稳胜 shim(155 vs 294 LOC),建议下一 nbhd 前先跑 architecture-v2 §6。§8 memory 三项对齐(GA-only / selling-only / 无双语 drawtext)。零 code 改动。/ docs/pipelines/reuse-report.md
[2026-07-11 16:32 UTC] C1: 落 neighborhoods/decatur.yaml(1.3KB, 7 wikimedia_queries + output_dir 隔离 + tag_hints + reel struct);静态分析 fetch_wikimedia.py 确认 QUERIES/OUT/TARGET 三处硬编码,零外部 config 消费 → "只加 config"目标破产,写 decatur-c1-attempt.md 记录破产点 4 条 + 给 C2 铺路(--neighborhood 参数化 / manifest 加 slug / 目录布局)。不改 py。/ docs/pipelines/neighborhoods/{decatur.yaml, decatur-c1-attempt.md}

